"use client"
import type { IEmployee } from "@/types"
export function EmployeeDetailPanel({ employee }: { employee: IEmployee | null }) {
  return <div className="p-6 text-center text-muted-foreground">Employee Detail — Coming Soon</div>
}